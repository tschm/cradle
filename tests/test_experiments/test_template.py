# from cvx.cradle.cli import cli
#
# from .utils import randomword
#
#
# def test_build(templates_dir):
#     # random name for the new repo
#     name = randomword(10, prefix='')
#     cli(template=str(templates_dir / "experiments"), user_defaults={"project_name": name})
